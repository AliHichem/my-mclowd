{% extends '::base.html.twig' %}

{% block content %}

<h3>{{ job.name }}</h3>

<p>{{ job.description }}</p>

<p>User: {{ job.user.username }}</p>

<a href="{{ path('app_proposals_new', {'job': job.id }) }}">Make Proposal</a>

{% endblock %}